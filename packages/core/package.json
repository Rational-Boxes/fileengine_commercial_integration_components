{
  "name": "@fileengine/embed-core",
  "version": "0.1.0",
  "description": "FileEngine embedding kit core — JSUM message bus + lazy hydration, API_REST client, SessionManager. Zero runtime dependencies (W3C Web Components only).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.js",
    "./jsum": "./src/jsum.js",
    "./api": "./src/api.js",
    "./session": "./src/session.js"
  },
  "files": ["src"],
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
